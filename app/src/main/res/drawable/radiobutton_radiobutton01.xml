<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:drawable="@drawable/btn_radio" android:state_pressed="true"/>
    <!-- Checked state -->
    <item android:drawable="@drawable/btn_radio" android:state_checked="true"/>
    <!-- Default state -->
    <item android:drawable="@drawable/btn_radio_gray"/>
</selector>